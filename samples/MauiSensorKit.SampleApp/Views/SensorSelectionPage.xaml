<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MauiSensorKit.SampleApp.ViewModels"
             x:Class="MauiSensorKit.SampleApp.Views.SensorSelectionPage"
             Title="Configure Sensors">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Auto Select" Command="{Binding AutoSelectAvailableCommand}" />
        <ToolbarItem Text="Enable All" Command="{Binding EnableAllCommand}" />
        <ToolbarItem Text="Disable All" Command="{Binding DisableAllCommand}" />
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto,Auto,*,Auto">
        <!-- Header -->
        <VerticalStackLayout Grid.Row="0" Padding="20,10" Spacing="5">
            <Label Text="Select Sensors to Record"
                   FontSize="24"
                   FontAttributes="Bold" />
            <Label Text="Choose which sensors to enable for data collection."
                   FontSize="14"
                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
        </VerticalStackLayout>

        <!-- Status Summary -->
        <Border Grid.Row="1"
                BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}"
                Padding="15,10"
                Margin="10,5"
                Stroke="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray700}}">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="8" />
            </Border.StrokeShape>
            <VerticalStackLayout Spacing="5">
                <Label Text="{Binding StatusSummary}"
                       FontSize="13"
                       FontAttributes="Bold"
                       HorizontalOptions="Center" />
                <Label Text="Green = Available | Yellow = Needs Permission | Red = Unavailable"
                       FontSize="11"
                       TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                       HorizontalOptions="Center" />
            </VerticalStackLayout>
        </Border>

        <!-- Sensor List -->
        <CollectionView Grid.Row="2"
                        ItemsSource="{Binding GroupedSensors}"
                        IsGrouped="True">
            <CollectionView.GroupHeaderTemplate>
                <DataTemplate>
                    <Border BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray800}}"
                            Padding="15,8"
                            Margin="10,5">
                        <Label Text="{Binding Name}"
                               FontSize="16"
                               FontAttributes="Bold" />
                    </Border>
                </DataTemplate>
            </CollectionView.GroupHeaderTemplate>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="15,10"
                            Margin="10,2"
                            Stroke="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray700}}"
                            StrokeThickness="1">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="8" />
                        </Border.StrokeShape>

                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="15">
                            <!-- Icon -->
                            <Image Grid.Column="0"
                                   Source="{Binding Icon}"
                                   WidthRequest="32"
                                   HeightRequest="32"
                                   Aspect="AspectFit" />

                            <!-- Info -->
                            <VerticalStackLayout Grid.Column="1" Spacing="2">
                                <Label Text="{Binding Name}"
                                       FontSize="16"
                                       FontAttributes="Bold" />
                                <Label Text="{Binding Description}"
                                       FontSize="12"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                                <HorizontalStackLayout Spacing="5">
                                    <Label Text="{Binding AvailabilityLabel}"
                                           FontSize="11"
                                           TextColor="{Binding AvailabilityColor}"
                                           FontAttributes="Italic" />
                                    <Label IsVisible="{Binding NeedsPermission}"
                                           Text="|"
                                           FontSize="11"
                                           TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray600}}" />
                                    <Label IsVisible="{Binding NeedsPermission}"
                                           Text="{Binding RequiredPermissions, StringFormat='Needs: {0}'}"
                                           FontSize="11"
                                           TextColor="#FF9800"
                                           FontAttributes="Italic" />
                                </HorizontalStackLayout>
                            </VerticalStackLayout>

                            <!-- Toggle or Permission Button -->
                            <Grid Grid.Column="2" ColumnDefinitions="Auto,Auto" ColumnSpacing="5">
                                <!-- Toggle for available sensors -->
                                <Switch Grid.Column="0"
                                        IsVisible="{Binding CanBeToggled}"
                                        IsToggled="{Binding IsEnabled, Mode=TwoWay}"
                                        IsEnabled="{Binding IsAvailable}"
                                        VerticalOptions="Center" />

                                <!-- Permission button for sensors needing permission -->
                                <Button Grid.Column="1"
                                        IsVisible="{Binding NeedsPermission}"
                                        Text="Allow"
                                        FontSize="11"
                                        Padding="10,6"
                                        BackgroundColor="#FF9800"
                                        TextColor="White"
                                        Command="{Binding BindingContext.RequestPermissionCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}"
                                        CommandParameter="{Binding .}"
                                        VerticalOptions="Center" />
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Footer -->
        <Button Grid.Row="3"
                Text="Save &amp; Continue"
                Command="{Binding SaveSelectionCommand}"
                Margin="20"
                Padding="15"
                FontSize="16"
                BackgroundColor="{StaticResource Primary}" />
    </Grid>

</ContentPage>
